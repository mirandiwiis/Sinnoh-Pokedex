import { Layout } from "../../components/layout/Layout";

export const Home = () => {
    // cosnt { pokemonList, error } = usePokemonList();

    return (
        <div>
            <Layout/>
        </div>
    );
};